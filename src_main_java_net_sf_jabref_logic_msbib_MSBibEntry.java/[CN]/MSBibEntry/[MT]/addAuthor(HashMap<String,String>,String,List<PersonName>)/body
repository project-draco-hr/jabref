{
  if (authors == null) {
    return;
  }
  String allAuthors="";
  boolean First=true;
  for (  PersonName name : authors) {
    if (!First) {
      allAuthors+=" and ";
    }
    allAuthors+=name.getFullname();
    First=false;
  }
  map.put(type,allAuthors);
}
