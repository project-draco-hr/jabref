{
  UndoableFieldChange edit=new UndoableFieldChange(entry,fileType.getFieldName(),entry.getField(fileType.getFieldName()),filename);
  entry.setField(fileType.getFieldName(),filename);
  panel.undoManager.addEdit(edit);
}
