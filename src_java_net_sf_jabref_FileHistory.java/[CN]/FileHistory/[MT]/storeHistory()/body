{
  if (history.size() > 0) {
    String[] names=new String[history.size()];
    for (int i=0; i < names.length; i++)     names[i]=(String)history.get(i);
    prefs.putStringArray("recentFiles",names);
  }
}
