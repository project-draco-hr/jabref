{
  if (add) {
    groups.add(index,regexp);
    groups.add(index,name);
    groups.add(index,field);
  }
 else {
    for (int i=0; i < GroupSelector.DIM; i++)     groups.removeElementAt(index);
  }
  if (revalidate)   gs.revalidateList();
}
