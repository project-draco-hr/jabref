{
  int score=0;
  Enumeration e=ruleSet.elements();
  while (e.hasMoreElements()) {
    score+=(((SearchRule)e.nextElement()).applyRule(searchString,bibtexEntry) > 0 ? 1 : 0);
  }
  if (and)   return ((score == ruleSet.size()) ? 1 : 0);
 else   return ((score > 0) ? 1 : 0);
}
