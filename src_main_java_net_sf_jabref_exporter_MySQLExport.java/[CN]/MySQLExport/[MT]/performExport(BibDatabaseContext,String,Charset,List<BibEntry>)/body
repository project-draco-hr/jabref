{
  Objects.requireNonNull(databaseContext);
  Objects.requireNonNull(entries);
  if (!entries.isEmpty()) {
    new DBExporterAndImporterFactory().getExporter(DatabaseType.MYSQL).exportDatabaseAsFile(databaseContext,entries,file,encodingToUse);
  }
}
