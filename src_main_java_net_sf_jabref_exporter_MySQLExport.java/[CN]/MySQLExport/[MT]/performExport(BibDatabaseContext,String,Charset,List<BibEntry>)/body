{
  Objects.requireNonNull(databaseContext);
  Objects.requireNonNull(entries);
  if (!entries.isEmpty()) {
    new DBExporterAndImporterFactory().getExporter("MYSQL").exportDatabaseAsFile(databaseContext,entries,file,encodingToUse);
  }
}
