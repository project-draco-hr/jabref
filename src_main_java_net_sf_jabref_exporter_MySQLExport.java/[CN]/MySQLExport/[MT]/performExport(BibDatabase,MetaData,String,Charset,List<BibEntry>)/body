{
  Objects.requireNonNull(database);
  Objects.requireNonNull(entries);
  if (!entries.isEmpty()) {
    new DBExporterAndImporterFactory().getExporter("MYSQL").exportDatabaseAsFile(database,metaData,entries,file,encodingToUse);
  }
}
