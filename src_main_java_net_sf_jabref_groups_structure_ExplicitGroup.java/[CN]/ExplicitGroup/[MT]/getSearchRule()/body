{
  return new SearchRule(){
    @Override public boolean applyRule(    String query,    BibEntry bibEntry){
      return contains(query,bibEntry);
    }
    @Override public boolean validateSearchStrings(    String query){
      return true;
    }
  }
;
}
