{
  String l;
  do {
    l=in.readLine();
    if (l == null) {
      return null;
    }
    if (l.indexOf("<pre>") != -1) {
      pre=true;
      l=in.readLine();
    }
    if (l.indexOf("</pre>") != -1) {
      pre=false;
    }
  }
 while (!pre);
  return l;
}
