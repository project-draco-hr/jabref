{
  set=new TreeSet(comp);
  BibtexEntry be;
  Set keySet=entries.keySet();
  if (keySet != null) {
    Iterator i=keySet.iterator();
    for (; i.hasNext(); ) {
      set.add(entries.get(i.next()));
    }
    index();
  }
}
