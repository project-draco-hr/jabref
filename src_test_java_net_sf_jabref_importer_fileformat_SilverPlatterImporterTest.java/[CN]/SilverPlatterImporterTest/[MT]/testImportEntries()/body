{
  try (InputStream in=SilverPlatterImporter.class.getResourceAsStream(txtName);InputStream bibIn=SilverPlatterImporterTest.class.getResourceAsStream(bibName)){
    List<BibEntry> entries=testImporter.importEntries(in,new OutputPrinterToNull());
    BibEntryAssert.assertEquals(bibIn,entries);
  }
 }
