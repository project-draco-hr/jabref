{
  if (containedInExplicitGroup(entry)) {
    JOptionPane.showMessageDialog(frame,"Groups assignment for this entry cannot be restored after next load due to lacking key",Globals.lang("Warning"),JOptionPane.WARNING_MESSAGE);
  }
}
