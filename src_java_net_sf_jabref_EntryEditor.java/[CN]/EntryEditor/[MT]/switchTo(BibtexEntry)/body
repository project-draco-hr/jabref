{
  entry=be;
  updateAllFields();
  updateSource();
  if (tabbed.getSelectedComponent() instanceof FieldPanel)   ((FieldPanel)tabbed.getSelectedComponent()).activate();
 else   ((JComponent)tabbed.getSelectedComponent()).requestFocus();
}
