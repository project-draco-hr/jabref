{
  if (entry == be)   return;
  storeCurrentEdit();
  entry.removePropertyChangeListener(this);
  be.addPropertyChangeListener(this);
  entry=be;
  updateAllFields();
  validateAllFields();
  updateSource();
  panel.showing=be;
}
