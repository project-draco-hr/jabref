{
  if (entry == be) {
    updateSource();
    return;
  }
  storeCurrentEdit();
  entry.removePropertyChangeListener(this);
  be.addPropertyChangeListener(this);
  entry=be;
  updateAllFields();
  validateAllFields();
  updateSource();
  panel.newEntryShowing(be);
}
