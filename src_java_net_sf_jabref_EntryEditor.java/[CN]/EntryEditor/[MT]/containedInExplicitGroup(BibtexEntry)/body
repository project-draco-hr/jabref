{
  AbstractGroup[] matchingGroups=panel.getGroupSelector().getGroupTreeRoot().getMatchingGroups(entry);
  for (int i=0; i < matchingGroups.length; ++i) {
    if (matchingGroups[i] instanceof ExplicitGroup)     return true;
  }
  return false;
}
