{
  if (tabbed.getSelectedComponent() == srcPanel) {
    updateField(source);
    if (lastSourceAccepted)     selectionListener.entryEditorClosing(EntryEditor.this);
  }
 else   selectionListener.entryEditorClosing(EntryEditor.this);
}
