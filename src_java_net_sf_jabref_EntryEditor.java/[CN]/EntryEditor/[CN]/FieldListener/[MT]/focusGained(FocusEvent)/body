{
  if (e.getSource() instanceof FieldEditor) {
    FieldEditor ta=(FieldEditor)e.getSource();
    Component parent=ta.getParent();
    while (!(parent instanceof FieldPanel))     parent=parent.getParent();
    ((FieldPanel)parent).setActive(ta);
  }
 else {
  }
}
