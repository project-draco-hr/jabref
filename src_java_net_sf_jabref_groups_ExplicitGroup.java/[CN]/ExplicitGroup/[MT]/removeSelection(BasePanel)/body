{
  BibtexEntry[] bes=basePanel.getSelectedEntries();
  Object bibtexkey;
  for (int i=0; i < bes.length; ++i) {
    bibtexkey=bes[i].getField("bibtexkey");
    if (bibtexkey != null)     m_entries.remove(bibtexkey);
  }
}
