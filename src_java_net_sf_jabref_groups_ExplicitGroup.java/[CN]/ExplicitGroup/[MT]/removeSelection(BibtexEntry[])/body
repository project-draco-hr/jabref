{
  if (entries.length == 0)   return null;
  HashSet entriesBeforeEdit=new HashSet(m_entries);
  for (int i=0; i < entries.length; ++i)   m_entries.remove(entries[i]);
  return new UndoableChangeAssignment(entriesBeforeEdit,m_entries);
}
