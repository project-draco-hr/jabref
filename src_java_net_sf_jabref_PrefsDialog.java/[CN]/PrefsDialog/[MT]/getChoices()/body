{
  int count=0;
  for (int i=0; i < _sel.length; i++)   if (_sel[i].booleanValue())   count++;
  String[] choices=new String[count];
  count=0;
  for (int i=0; i < _sel.length; i++)   if (_sel[i].booleanValue()) {
    choices[count]=GUIGlobals.ALL_FIELDS[i];
    count++;
  }
  return choices;
}
