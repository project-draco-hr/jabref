{
  int count=0;
  for (int i=0; i < sel_ac.length; i++)   if (sel_ac[i].isSelected())   count++;
  byte[] choices=new byte[count];
  count=0;
  for (int i=0; i < sel_ac.length; i++)   if (sel_ac[i].isSelected()) {
    choices[count]=(byte)i;
    count++;
  }
  return choices;
}
