{
  if (!goOn)   return;
  panel.output(Globals.lang("Finished synchronizing %0 links. Entries changed: %1.",new String[]{fieldName.toUpperCase(),String.valueOf(entriesChanged)}));
  if (entriesChanged > 0) {
    panel.markBaseChanged();
    panel.refreshTable();
  }
}
