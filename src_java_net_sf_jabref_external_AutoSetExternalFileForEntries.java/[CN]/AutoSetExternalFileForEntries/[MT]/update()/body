{
  if (!goOn)   return;
  panel.output(Globals.lang("Finished autosetting %0 field. Entries changed: %1.",new String[]{fieldName,String.valueOf(set)}));
  if (set > 0) {
    panel.markBaseChanged();
    panel.refreshTable();
  }
}
