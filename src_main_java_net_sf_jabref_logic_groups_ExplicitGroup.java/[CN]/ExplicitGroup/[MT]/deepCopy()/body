{
  try {
    ExplicitGroup copy=new ExplicitGroup(getName(),getContext());
    copy.legacyEntryKeys.addAll(legacyEntryKeys);
    return copy;
  }
 catch (  ParseException exception) {
    LOGGER.error("Internal error in ExplicitGroup.deepCopy(). " + "Please report this on https://github.com/JabRef/jabref/issues",exception);
    return null;
  }
}
