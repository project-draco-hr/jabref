{
  LinkedList<TMarkedStoreItem> ll=tagMap.get(tag);
  if (ll == null) {
    ll=new LinkedList<>();
    tagMap.put(tag,ll);
  }
  TMarkedStoreItem item=new TMarkedStoreItem();
  ll.add(item);
  item.end=end;
  item.start=start;
}
