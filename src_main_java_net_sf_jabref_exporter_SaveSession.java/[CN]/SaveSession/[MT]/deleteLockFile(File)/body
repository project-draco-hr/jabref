{
  File lock=new File(file.getPath() + SaveSession.LOCKFILE_SUFFIX);
  if (!lock.exists()) {
    return false;
  }
  if (!lock.delete()) {
    LOGGER.info("Cannot delete lock file");
  }
  return true;
}
