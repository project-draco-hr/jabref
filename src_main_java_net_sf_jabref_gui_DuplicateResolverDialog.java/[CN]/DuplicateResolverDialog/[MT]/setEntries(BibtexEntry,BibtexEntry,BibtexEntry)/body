{
  setSourceView(newOne,newTwo,newThree);
  p1.setEntry(newOne);
  p2.setEntry(newTwo);
  p3.setEntry(newThree);
  status=DuplicateResolverDialog.NOT_CHOSEN;
  p1.revalidate();
  p1.repaint();
  block=true;
}
