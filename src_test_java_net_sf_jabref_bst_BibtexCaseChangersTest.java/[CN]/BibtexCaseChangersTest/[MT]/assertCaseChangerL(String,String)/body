{
  Assert.assertEquals(string,BibtexCaseChanger.changeCase(string2,'l',new Warn(){
    @Override public void warn(    String s){
      Assert.fail("Should not Warn (" + s + ")! changeCase('l') should be "+ string+ " for "+ string2);
    }
  }
));
}
