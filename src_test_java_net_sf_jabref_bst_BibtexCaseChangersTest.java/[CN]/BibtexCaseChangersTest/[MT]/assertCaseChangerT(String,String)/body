{
  Assert.assertEquals(string,BibtexCaseChanger.changeCase(string2,'t',new Warn(){
    @Override public void warn(    String s){
      Assert.fail("Should not Warn (" + s + ")! changeCase('t') should be "+ string+ " for "+ string2);
    }
  }
));
}
