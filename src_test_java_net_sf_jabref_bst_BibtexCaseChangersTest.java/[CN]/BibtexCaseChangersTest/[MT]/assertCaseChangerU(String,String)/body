{
  Assert.assertEquals(string,BibtexCaseChanger.changeCase(string2,'u',new Warn(){
    @Override public void warn(    String s){
      Assert.fail("Should not Warn (" + s + ")! changeCase('u') should be "+ string+ " for "+ string2);
    }
  }
));
}
