{
synchronized (set) {
    if (changed) {
      Collections.sort(set,comp);
      changed=false;
    }
    int count=set.size();
    entryArray=new BibEntry[count];
    int piv=0;
    for (    BibEntry entry : set) {
      entryArray[piv]=entry;
      piv++;
    }
  }
}
