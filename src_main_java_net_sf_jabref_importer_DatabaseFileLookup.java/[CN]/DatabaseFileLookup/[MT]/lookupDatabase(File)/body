{
  if (cache.containsKey(file)) {
    return cache.get(file);
  }
 else {
    boolean res=false;
    for (    BibEntry entry : entries) {
      if (lookupEntry(file,entry)) {
        res=true;
        break;
      }
    }
    cache.put(file,res);
    return res;
  }
}
