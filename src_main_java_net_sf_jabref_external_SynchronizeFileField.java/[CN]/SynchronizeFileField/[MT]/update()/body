{
  if (!goOn) {
    return;
  }
  panel.output(Localization.lang("Finished synchronizing %0 links. Entries changed: %1.",Globals.FILE_FIELD.toUpperCase(),String.valueOf(entriesChangedCount)));
  panel.frame().setProgressBarVisible(false);
  if (entriesChangedCount > 0) {
    panel.markBaseChanged();
  }
}
