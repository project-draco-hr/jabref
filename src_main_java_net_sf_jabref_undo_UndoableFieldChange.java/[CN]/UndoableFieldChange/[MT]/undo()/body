{
  super.undo();
  try {
    if (oldValue != null) {
      entry.setField(field,oldValue);
    }
 else {
      entry.clearField(field);
    }
  }
 catch (  Throwable ex) {
    Util.pr(ex.getMessage());
  }
}
