{
  if (array.length == 0)   purge(0);
 else {
    for (int i=0; i < array.length; i++) {
      Globals.prefs.putStringArray("customExportFormat" + i,(String[])(array[i]));
    }
    purge(array.length);
  }
}
