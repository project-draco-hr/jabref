{
  String res=getLastOnly();
  if (abbr) {
    res=(firstAbbr == null ? "" : firstAbbr + ' ') + res;
  }
 else {
    res=(firstPart == null ? "" : firstPart + ' ') + res;
  }
  if (jrPart != null) {
    res+=", " + jrPart;
  }
  return res;
}
