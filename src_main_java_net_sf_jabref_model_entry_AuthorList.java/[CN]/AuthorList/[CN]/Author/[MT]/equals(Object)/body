{
  if (!(o instanceof Author)) {
    return false;
  }
  Author a=(Author)o;
  return CompareUtil.equals(firstPart,a.firstPart) && CompareUtil.equals(firstAbbr,a.firstAbbr) && CompareUtil.equals(vonPart,a.vonPart)&& CompareUtil.equals(lastPart,a.lastPart)&& CompareUtil.equals(jrPart,a.jrPart);
}
