{
  if (!(o instanceof Author)) {
    return false;
  }
  Author a=(Author)o;
  return Util.equals(firstPart,a.firstPart) && Util.equals(firstAbbr,a.firstAbbr) && Util.equals(vonPart,a.vonPart)&& Util.equals(lastPart,a.lastPart)&& Util.equals(jrPart,a.jrPart);
}
