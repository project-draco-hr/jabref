{
  authors=new Vector<>(5);
  original=bibtexAuthors;
  tokenStart=0;
  tokenEnd=0;
  while (tokenStart < original.length()) {
    Author author=getAuthor();
    if (author != null) {
      authors.add(author);
    }
  }
  original=null;
  tokens=null;
}
