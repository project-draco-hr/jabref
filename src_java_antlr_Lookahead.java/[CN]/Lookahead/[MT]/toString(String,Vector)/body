{
  String b, f="", d="";
  b=fset.toString(separator,vocab);
  if (cycle != null) {
    f="; FOLLOW(" + cycle + ")";
  }
  if (epsilonDepth != null) {
    d="; depths=" + epsilonDepth.toString(",");
  }
  return b + f + d;
}
