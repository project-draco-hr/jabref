{
  if (command == null) {
    return;
  }
  Future<?> future=executorService.submit(command);
  while (true) {
    try {
      future.get();
      return;
    }
 catch (    InterruptedException|ExecutionException e) {
      e.printStackTrace();
    }
  }
}
