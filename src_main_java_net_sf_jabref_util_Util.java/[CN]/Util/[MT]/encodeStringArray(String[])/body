{
  StringBuilder sb=new StringBuilder();
  for (int i=0; i < entry.length; i++) {
    sb.append(Util.encodeString(entry[i]));
    if (i < (entry.length - 1)) {
      sb.append(':');
    }
  }
  return sb.toString();
}
