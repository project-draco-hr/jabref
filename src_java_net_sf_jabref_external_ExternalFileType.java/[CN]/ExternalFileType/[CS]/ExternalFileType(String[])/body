{
  if ((val == null) || (val.length < 4))   throw new IllegalArgumentException("Cannot contruct ExternalFileType without four elements in String[] argument.");
  this.name=val[0];
  this.extension=val[1];
  this.openWith=val[2];
  this.iconName=val[3];
  this.icon=GUIGlobals.getImage(val[3]);
}
