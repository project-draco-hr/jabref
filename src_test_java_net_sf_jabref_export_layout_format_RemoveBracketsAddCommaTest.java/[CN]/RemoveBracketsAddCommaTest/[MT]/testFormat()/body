{
  Assert.assertEquals("some text,",formatter.format("{some text}"));
  Assert.assertEquals("some text",formatter.format("{some text"));
  Assert.assertEquals("some text,",formatter.format("some text}"));
}
