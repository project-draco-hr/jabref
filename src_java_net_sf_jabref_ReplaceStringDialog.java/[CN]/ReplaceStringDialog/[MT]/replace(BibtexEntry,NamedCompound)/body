{
  int counter=0;
  if (allFields()) {
    Object[] os=be.getAllFields();
    for (int i=0; i < os.length; i++) {
      String s=(String)os[i];
      if (!s.equals(Globals.KEY_FIELD))       counter+=replaceField(be,s,ce);
    }
  }
 else {
    for (int i=0; i < flds.length; i++) {
      if (!flds[i].equals(Globals.KEY_FIELD))       counter+=replaceField(be,flds[i],ce);
    }
  }
  return counter;
}
