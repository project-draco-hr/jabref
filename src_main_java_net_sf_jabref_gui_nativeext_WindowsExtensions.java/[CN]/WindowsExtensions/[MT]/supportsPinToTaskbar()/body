{
  if (!OS.WINDOWS) {
    return false;
  }
  try {
    Float version=Float.parseFloat(System.getProperty("os.version"));
    return version >= 6.1;
  }
 catch (  NumberFormatException ex) {
    return false;
  }
}
