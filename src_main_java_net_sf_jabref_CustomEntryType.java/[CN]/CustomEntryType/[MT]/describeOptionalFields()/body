{
  StringBuffer sb=new StringBuffer();
  for (int i=0; i < opt.length; i++) {
    sb.append(opt[i]);
    sb.append(((i <= (opt.length - 1)) && (opt.length > 1)) ? ", " : "");
  }
  return sb.toString();
}
