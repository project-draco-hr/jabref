{
  StringBuilder sb=new StringBuilder();
  for (int i=0; i < req.length; i++) {
    sb.append(req[i]);
    sb.append(i <= req.length - 1 && req.length > 1 ? ", " : "");
  }
  return sb.toString();
}
