{
  name=name_;
  if (reqStr.isEmpty()) {
    req=new String[0];
  }
 else {
    parseRequiredFields(reqStr);
  }
  if (optStr.isEmpty()) {
    opt=new String[0];
  }
 else {
    opt=optStr.split(";");
  }
}
