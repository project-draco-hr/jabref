{
switch (version) {
case 0:
case 1:
    return Version0_1.fromString(orderedData.get(0),version);
case 2:
case 3:
  return Version2_3.fromString(orderedData,version);
default :
throw new IllegalArgumentException("Failed to read groups data (unsupported version: " + Integer.toString(version) + ")");
}
}
