{
  if (Globals.ON_WIN) {
    String progFiles=System.getenv("ProgramFiles(x86)");
    if (progFiles == null)     progFiles=System.getenv("ProgramFiles");
    return progFiles + "\\texstudio\\texstudio.exe";
  }
 else {
    return "texstudio";
  }
}
