{
  int index=-1;
  for (int i=OFFSET; i < groups.size(); i+=DIM) {
    if (name.toLowerCase().compareTo(((String)groups.elementAt(i + 1)).toLowerCase()) < 0) {
      index=i;
      i=groups.size();
    }
  }
  if (index == -1) {
    index=groups.size();
  }
  return index;
}
