{
  Object[] newPath=new Object[path.length - 1];
  for (int oldIdx=path.length - 1, newIdx=newPath.length - 1; oldIdx >= 0; --oldIdx, --newIdx) {
    if (newIdx < 0)     return path;
    newPath[newIdx]=path[oldIdx];
    if (path[oldIdx] == node)     --oldIdx;
  }
  return newPath;
}
