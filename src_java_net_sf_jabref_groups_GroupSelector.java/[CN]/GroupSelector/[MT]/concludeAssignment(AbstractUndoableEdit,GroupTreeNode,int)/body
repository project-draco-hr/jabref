{
  panel.undoManager.addEdit(undo);
  panel.markBaseChanged();
  frame.output("Assigned " + numberOfEntries + " "+ (numberOfEntries == 1 ? "entry" : "entries")+ " to group \""+ node.getGroup().getName()+ "\"");
}
