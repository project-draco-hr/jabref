{
  StringBuffer sb=new StringBuffer();
  String[] authors=in.split(" and ");
  for (int i=0; i < authors.length; i++) {
    String[] t=authors[i].split(",");
    if (t.length < 2)     return in;
    sb.append(t[1].trim() + " " + t[0].trim());
    if (i == authors.length - 1)     sb.append(".");
 else     sb.append(" and ");
  }
  return sb.toString();
}
