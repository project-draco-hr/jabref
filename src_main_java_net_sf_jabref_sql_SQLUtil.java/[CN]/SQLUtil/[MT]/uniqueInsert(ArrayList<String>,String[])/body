{
  if (array != null) {
    for (    String anArray : array) {
      if (!list.contains(anArray)) {
        if (!"#".equals(anArray)) {
          list.add(anArray);
        }
      }
    }
  }
  return list;
}
