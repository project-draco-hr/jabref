{
  String fieldstr="";
  String field;
  for (int i=0; i < getAllFields().size(); i++) {
    field=allFields.get(i);
    if (i > 0)     fieldstr=fieldstr + ", ";
    if (reservedDBWords.contains(field))     field+="_";
    fieldstr=fieldstr + field;
  }
  return fieldstr;
}
