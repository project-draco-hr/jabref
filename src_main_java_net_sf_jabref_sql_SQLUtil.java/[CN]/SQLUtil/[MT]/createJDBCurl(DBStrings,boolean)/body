{
  DBStringsPreferences preferences=dbStrings.getDbPreferences();
  return "jdbc:" + preferences.getServerType().getFormattedName().toLowerCase() + "://"+ preferences.getServerHostname()+ (withDBName ? '/' + preferences.getDatabase() : "")+ dbStrings.getDbParameters();
}
