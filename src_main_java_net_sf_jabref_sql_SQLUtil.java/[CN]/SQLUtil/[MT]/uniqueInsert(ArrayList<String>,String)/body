{
  if (array != null) {
    for (    String anArray : array) {
      if (!list.contains(anArray)) {
        if (!anArray.equals("#")) {
          list.add(anArray);
        }
      }
    }
  }
  return list;
}
