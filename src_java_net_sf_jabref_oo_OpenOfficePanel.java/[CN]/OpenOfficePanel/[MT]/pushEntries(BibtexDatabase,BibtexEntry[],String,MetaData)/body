{
  if (ooBase == null) {
    connect(true);
  }
  if (ooBase != null) {
    if (style == null)     try {
      readStyleFile();
      pushEntries(Globals.prefs.getBoolean("ooInParCitation"),entries);
    }
 catch (    Exception ex) {
      ex.printStackTrace();
    }
  }
}
