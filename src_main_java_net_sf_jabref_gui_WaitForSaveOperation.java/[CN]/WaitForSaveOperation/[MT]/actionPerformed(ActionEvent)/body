{
  boolean anySaving=false;
  for (int i=0; i < frame.getBasePanelCount(); i++) {
    if (frame.getBasePanelAt(i).isSaving()) {
      anySaving=true;
      break;
    }
  }
  if (!anySaving) {
    t.stop();
    diag.dispose();
  }
}
