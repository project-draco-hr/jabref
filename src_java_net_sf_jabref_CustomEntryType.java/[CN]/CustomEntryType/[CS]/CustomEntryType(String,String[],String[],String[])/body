{
  name=name_;
  parseRequiredFields(req_);
  ArrayList<String> allOpt=new ArrayList<String>();
  for (int i=0; i < opt_.length; i++)   allOpt.add(opt_[i]);
  for (int i=0; i < opt2_.length; i++)   allOpt.add(opt2_[i]);
  opt=allOpt.toArray(new String[allOpt.size()]);
  priOpt=opt_;
}
