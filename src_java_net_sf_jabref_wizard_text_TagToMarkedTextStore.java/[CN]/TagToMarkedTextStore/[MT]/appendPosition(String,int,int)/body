{
  LinkedList ll;
  Object dummy=tagMap.get(tag);
  if (dummy == null) {
    ll=new LinkedList();
    tagMap.put(tag,ll);
  }
 else {
    ll=(LinkedList)dummy;
  }
  TMarkedStoreItem item=new TMarkedStoreItem();
  ll.add(item);
  item.end=end;
  item.start=start;
}
