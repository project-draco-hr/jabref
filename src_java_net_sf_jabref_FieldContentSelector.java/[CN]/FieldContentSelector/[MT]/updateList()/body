{
  list.removeAllItems();
  list.addItem("");
  Vector items=metaData.getData(Globals.SELECTOR_META_PREFIX + editor.getFieldName());
  if ((items != null) && (items.size() > 0)) {
    for (int i=0; i < items.size(); i++)     list.addItem(items.elementAt(i));
  }
}
