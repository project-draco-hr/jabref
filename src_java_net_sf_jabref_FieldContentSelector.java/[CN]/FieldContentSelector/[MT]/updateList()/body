{
  list.removeAllItems();
  list.addItem("");
  Vector items=m_metaData.getData(Globals.SELECTOR_META_PREFIX + m_editor.getFieldName());
  if ((items != null) && (items.size() > 0)) {
    for (int i=0; i < items.size(); i++)     list.addItem(items.elementAt(i));
  }
}
