{
  if (!checkCurrentConnection()) {
    return;
  }
  try {
    metaData.setParsedData(MetaDataParser.getParsedData(dbmsProcessor.getSharedMetaData(),keywordSeparator,metaData));
  }
 catch (  ParseException e) {
    LOGGER.error("Parse error",e);
  }
}
