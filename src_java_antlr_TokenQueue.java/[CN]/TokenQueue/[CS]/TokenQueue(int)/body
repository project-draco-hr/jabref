{
  int size;
  if (minSize < 0) {
    init(16);
    return;
  }
  if (minSize >= (Integer.MAX_VALUE / 2)) {
    init(Integer.MAX_VALUE);
    return;
  }
  for (size=2; size < minSize; size*=2) {
    ;
  }
  init(size);
}
