{
  if (fieldText == null) {
    return null;
  }
  if (fieldText.trim().isEmpty()) {
    return "";
  }
  if (DOIUtil.isURI(fieldText)) {
    return fieldText;
  }
 else {
    return DOIUtil.getURI(fieldText);
  }
}
