{
  if (fieldText == null) {
    return null;
  }
  if (fieldText.trim().isEmpty()) {
    return "";
  }
  if (Doi.containsHttpDoi(fieldText)) {
    return fieldText;
  }
 else {
    return new Doi(fieldText).getUri();
  }
}
