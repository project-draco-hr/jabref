{
  StringBuilder sb=new StringBuilder("<html>");
  sb.append(Globals.lang("This database was written using an older version of JabRef."));
  sb.append("</html>");
  int answer=JOptionPane.showConfirmDialog(panel.frame(),sb.toString());
  if (answer == JOptionPane.OK_OPTION)   makeChanges(panel,pr);
}
