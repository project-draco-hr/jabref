{
  for (Iterator iterator=database.getEntries().iterator(); iterator.hasNext(); ) {
    BibtexEntry entry=(BibtexEntry)iterator.next();
    for (int i=0; i < fields.length; i++) {
      if (entry.getField(fields[i]) != null)       return true;
    }
  }
  return false;
}
