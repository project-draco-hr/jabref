{
  assertCorrect(createContext("title","Only ascii characters!'@12"));
  assertWrong(createContext("month","Umlauts are n??t ??llowed"));
  assertWrong(createContext("author","Some unicode ???"));
}
