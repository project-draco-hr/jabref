{
  List<IntegrityMessage> messages=new IntegrityCheck(context,JabRefPreferences.getInstance().getFileDirectoryPreferences()).checkBibtexDatabase();
  assertEquals(Collections.emptyList(),messages);
}
