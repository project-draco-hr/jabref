{
switch (mode) {
case FLOAT:
    updateFloat();
  break;
case FILTER:
updateFilter();
break;
case GLOBAL:
updateGlobal();
break;
}
frame.getSearchBar().updateResults(hits,searchQuery.description);
}
