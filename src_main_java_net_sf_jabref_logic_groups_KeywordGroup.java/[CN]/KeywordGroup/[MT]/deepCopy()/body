{
  try {
    return new KeywordGroup(getName(),searchField,searchExpression,caseSensitive,regExp,getContext());
  }
 catch (  Throwable t) {
    LOGGER.error("Internal error in KeywordGroup.deepCopy(). " + "Please report this on https://github.com/JabRef/jabref/issues",t);
    return null;
  }
}
