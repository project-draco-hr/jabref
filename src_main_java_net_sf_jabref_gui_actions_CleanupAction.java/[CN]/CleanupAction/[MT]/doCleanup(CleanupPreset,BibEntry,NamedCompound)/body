{
  CleanupWorker cleaner=new CleanupWorker(preset,Arrays.asList(panel.metaData().getFileDirectory(Globals.FILE_FIELD)));
  List<FieldChange> changes=cleaner.cleanup(entry);
  if (changes.isEmpty()) {
    return;
  }
  for (  FieldChange change : changes) {
    ce.addEdit(new UndoableFieldChange(change));
  }
}
