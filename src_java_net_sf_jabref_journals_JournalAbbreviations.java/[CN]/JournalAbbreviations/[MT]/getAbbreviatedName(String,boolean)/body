{
  String s=journalName.toLowerCase();
  Object o=fullNameKeyed.get(s);
  if (o == null)   return null;
  s=(String)o;
  return titleCase ? caseChanger.changeCase(s,CaseChanger.UPPER_EACH_FIRST) : caseChanger.changeCase(s,CaseChanger.UPPER_FIRST);
}
