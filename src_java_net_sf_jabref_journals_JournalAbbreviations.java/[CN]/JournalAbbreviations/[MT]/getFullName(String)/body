{
  String s=getAbbreviatedName(journalName,true).toLowerCase();
  Object o=abbrNameKeyed.get(s);
  if (o == null) {
    if (fullNameKeyed.containsKey(s))     o=s;
 else     return null;
  }
  s=(String)o;
  return caseChanger.changeCase(s,CaseChanger.UPPER_EACH_FIRST);
}
