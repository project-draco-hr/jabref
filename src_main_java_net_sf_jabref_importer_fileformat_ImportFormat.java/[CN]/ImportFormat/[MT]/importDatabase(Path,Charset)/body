{
  try (InputStream stream=new FileInputStream(filePath.toFile());BufferedReader bufferedReader=new BufferedReader(new InputStreamReader(stream,defaultEncoding))){
    return importDatabase(bufferedReader);
  }
 }
