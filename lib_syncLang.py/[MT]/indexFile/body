def indexFile(lines):
    allKeys = []
    for line in lines:
        comment = line.find('#')
        index = line.find('=')
        if ((comment != 0) and (index > 0)):
            allKeys.append(line[0:index])
    allKeys.sort()
    return allKeys
