{
  AbstractGroup group=node.getGroup();
  AbstractAction action=add ? (AbstractAction)new AddToGroupAction(group) : (AbstractAction)new RemoveFromGroupAction(group);
  action.setEnabled(add ? group.supportsAdd() : group.supportsRemove());
  return action;
}
