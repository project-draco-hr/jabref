{
  if (e1 == e2)   return 0;
  int mrk1=Util.isMarked(e1), mrk2=Util.isMarked(e2);
  if (mrk1 == mrk2)   return (next != null ? next.compare(e1,e2) : idCompare(e1,e2));
 else   return mrk2 - mrk1;
}
