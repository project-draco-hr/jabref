{
  BibtexEntry e1=(BibtexEntry)o1, e2=(BibtexEntry)o2;
  if (e1 == e2)   return 0;
  boolean mrk1=Util.isMarked(e1), mrk2=Util.isMarked(e2);
  if (mrk1 == mrk2)   return (next != null ? next.compare(o1,o2) : idCompare(e1,e2));
 else   if (mrk2)   return 1;
 else   return -1;
}
