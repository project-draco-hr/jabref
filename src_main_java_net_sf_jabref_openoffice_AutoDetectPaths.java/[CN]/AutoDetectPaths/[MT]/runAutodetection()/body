{
  try {
    if (OpenOfficePreferences.checkAutoDetectedPaths()) {
      return true;
    }
    init();
    getWorker().run();
    update();
    return foundPaths;
  }
 catch (  Throwable e) {
    LOGGER.warn("Problem when auto-detecting paths",e);
    return false;
  }
}
