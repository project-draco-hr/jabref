{
  int thisRow=panel.tableModel.getNumberFromName(entry.getId());
  String id=null;
  if (thisRow + 1 < panel.database.getEntryCount())   id=panel.tableModel.getNameFromNumber(thisRow + 1);
 else   if (thisRow > 0)   id=panel.tableModel.getNameFromNumber(0);
  switchTo(id);
}
