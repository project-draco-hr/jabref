{
  String suffix="";
  for (int i=maxFieldLength - field.length(); i > 0; i--)   suffix+=" ";
  if (tagDisplayNameMap.containsKey(field.toLowerCase()))   return tagDisplayNameMap.get(field.toLowerCase()) + suffix;
  return (field.charAt(0) + "").toUpperCase() + field.substring(1) + suffix;
}
