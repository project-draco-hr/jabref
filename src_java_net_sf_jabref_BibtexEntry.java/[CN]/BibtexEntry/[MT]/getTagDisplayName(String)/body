{
  String suffix="";
  for (int i=maxFieldLength - tag.length(); i > 0; i--)   suffix+=" ";
  if (tagDisplayNameMap.containsKey(tag.toLowerCase()))   return tagDisplayNameMap.get(tag.toLowerCase()) + suffix;
  return (tag.charAt(0) + "").toUpperCase() + tag.substring(1) + suffix;
}
