{
switch (version) {
case 0:
case 1:
    return Version0_1.fromString((String)orderedData.firstElement(),db,version);
default :
  throw new IllegalArgumentException("Failed to read groups data (version: " + version + " is not supported)");
}
}
