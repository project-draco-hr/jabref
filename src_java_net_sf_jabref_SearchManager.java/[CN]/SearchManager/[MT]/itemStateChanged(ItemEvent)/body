{
  if (e.getSource() == increment) {
    if (increment.isSelected())     searchField.addKeyListener(ths);
 else     searchField.removeKeyListener(ths);
  }
 else   if (e.getSource() == reorder) {
    if (!reorder.isSelected()) {
      frame.stopShowingSearchResults();
    }
  }
}
