{
  GroupTreeNode node=getNodeInSimpleTree();
  AbstractGroup newGroup=new ExplicitGroup("NewGroup",GroupHierarchyType.INDEPENDENT,JabRefPreferences.getInstance().get(JabRefPreferences.KEYWORD_SEPARATOR));
  node.setGroup(newGroup,true,entries);
  assertEquals(newGroup,node.getGroup());
}
