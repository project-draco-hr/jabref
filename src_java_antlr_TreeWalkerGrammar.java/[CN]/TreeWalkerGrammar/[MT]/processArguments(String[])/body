{
  for (int i=0; i < args.length; i++) {
    if (args[i].equals("-trace")) {
      traceRules=true;
      Tool.setArgOK(i);
    }
 else     if (args[i].equals("-traceTreeParser")) {
      traceRules=true;
      Tool.setArgOK(i);
    }
  }
}
