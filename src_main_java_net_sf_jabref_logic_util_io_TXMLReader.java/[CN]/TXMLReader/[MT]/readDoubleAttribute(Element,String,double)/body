{
  if (node != null) {
    String data=node.getAttribute(attrName);
    if (data != null) {
      if (!data.isEmpty()) {
        return Double.parseDouble(data);
      }
    }
  }
  return defaultValue;
}
