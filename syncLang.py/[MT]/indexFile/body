def indexFile(lines):
    allKeys = []
    for line in lines:
        comment = line.find('#')
        if (comment != 0):
            index = line.find('=')
            while ((index > 0) and (line[(index - 1)] == '\\')):
                index = line.find('=', (index + 1))
            if (index > 0):
                allKeys.append(line[0:index])
    allKeys.sort()
    return allKeys
