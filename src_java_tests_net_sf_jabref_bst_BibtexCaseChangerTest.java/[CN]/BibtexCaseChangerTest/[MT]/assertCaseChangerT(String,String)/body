{
  assertEquals(string,BibtexCaseChanger.changeCase(string2,'t',new Warn(){
    public void warn(    String s){
      fail("Should not Warn (" + s + ")! changeCase('t') should be "+ string+ " for "+ string2);
    }
  }
));
}
