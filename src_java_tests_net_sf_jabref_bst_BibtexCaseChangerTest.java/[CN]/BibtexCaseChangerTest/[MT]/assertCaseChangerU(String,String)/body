{
  assertEquals(string,BibtexCaseChanger.changeCase(string2,'u',new Warn(){
    public void warn(    String s){
      fail("Should not Warn (" + s + ")! changeCase('u') should be "+ string+ " for "+ string2);
    }
  }
));
}
