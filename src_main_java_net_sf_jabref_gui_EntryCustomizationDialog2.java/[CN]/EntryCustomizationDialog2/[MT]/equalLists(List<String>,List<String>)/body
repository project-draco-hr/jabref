{
  if ((one == null) && (two == null)) {
    return true;
  }
  if ((one == null) || (two == null)) {
    return false;
  }
  if (one.size() != two.size()) {
    return false;
  }
  for (int i=0; i < one.size(); i++) {
    if (!one.get(i).equals(two.get(i))) {
      return false;
    }
  }
  return true;
}
