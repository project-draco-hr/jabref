{
  for (java.util.Iterator i=_strings.iterator(); i.hasNext(); ) {
    if (((BibtexString)i.next()).getName().equals(string.getName()))     throw new KeyCollisionException("A string with this label already exists,");
  }
  _strings.insertElementAt(string,index);
}
