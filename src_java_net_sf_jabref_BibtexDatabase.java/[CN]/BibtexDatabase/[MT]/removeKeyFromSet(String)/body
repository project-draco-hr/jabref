{
  if ((key == null) || key.equals(""))   return;
  if (allKeys.containsKey(key)) {
    Integer tI=allKeys.get(key);
    if (tI.intValue() == 1)     allKeys.remove(key);
 else     allKeys.put(key,new Integer((tI).intValue() - 1));
  }
}
