{
  super(frame,Globals.lang("Set general fields"),false);
  parent=frame;
  this.diag=diag;
  try {
    jbInit();
    setSize(new Dimension(400,200));
  }
 catch (  Exception ex) {
    ex.printStackTrace();
  }
}
