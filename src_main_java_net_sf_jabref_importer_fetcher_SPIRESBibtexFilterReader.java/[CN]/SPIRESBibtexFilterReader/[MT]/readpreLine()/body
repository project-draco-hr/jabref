{
  String l;
  do {
    l=inBR.readLine();
    if (l == null) {
      return null;
    }
    if (l.equals("<pre>")) {
      pre=true;
      l=inBR.readLine();
    }
    if (l.equals("</pre>")) {
      pre=false;
    }
  }
 while (!pre);
  return l;
}
