{
  String l;
  do {
    l=inBR.readLine();
    if (l == null) {
      return null;
    }
    if ("<pre>".equals(l)) {
      pre=true;
      l=inBR.readLine();
    }
    if ("</pre>".equals(l)) {
      pre=false;
    }
  }
 while (!pre);
  return l;
}
