{
  String key=fieldName + "Directory";
  String dir;
  Vector vec=getData(key);
  if ((vec != null) && (vec.size() > 0))   dir=(String)vec.get(0);
 else   dir=Globals.prefs.get(key);
  return dir;
}
