{
  String result=new String();
  int bs_size=bs.size();
  for (int i=0; i < bs_size; i++) {
    if (bs.member(i))     result+="true";
 else     result+="false";
    if (i < bs_size - 1)     result+=separator;
  }
  return result;
}
