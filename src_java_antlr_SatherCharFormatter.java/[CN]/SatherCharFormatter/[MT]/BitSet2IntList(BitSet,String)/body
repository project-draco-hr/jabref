{
  String result=new String();
  boolean first=true;
  for (int i=0; i < bs.size(); i++)   if (bs.member(i)) {
    if (!first)     result+=separator;
 else     first=false;
    result+=i;
  }
  return result;
}
