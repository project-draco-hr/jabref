{
  int i=0, count=0;
  while (i < s.length()) {
    if (s.charAt(i) == '{') {
      count++;
    }
 else     if (s.charAt(i) == '}') {
      count--;
    }
    if (count < 0) {
      return true;
    }
    i++;
  }
  return false;
}
