{
  try (InputStream in=RepecNepImporter.class.getResourceAsStream("RepecNepImporterTest1.txt");InputStream bibIn=RepecNepImporter.class.getResourceAsStream("RepecNepImporterTest1.bib")){
    List<BibEntry> entries=testImporter.importDatabase(in).getDatabase().getEntries();
    Assert.assertEquals(1,entries.size());
    BibtexEntryAssert.assertEquals(bibIn,entries.get(0));
  }
 }
