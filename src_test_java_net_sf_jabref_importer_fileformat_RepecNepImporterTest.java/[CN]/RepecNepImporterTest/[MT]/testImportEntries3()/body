{
  try (InputStream in=RepecNepImporter.class.getResourceAsStream("RepecNepImporterTest3.txt");InputStream bibIn=RepecNepImporter.class.getResourceAsStream("RepecNepImporterTest3.bib")){
    List<BibEntry> entries=testImporter.importEntries(in,new OutputPrinterToNull());
    Assert.assertEquals(1,entries.size());
    BibEntryAssert.assertEquals(bibIn,entries.get(0));
  }
 }
