{
  filenm=filenm.toLowerCase();
  int dotPos=filenm.lastIndexOf(".");
  if (dotPos == -1) {
    return false;
  }
  int dotDotPos=filenm.lastIndexOf(".",dotPos - 1);
  return extSet.contains(filenm.substring(dotPos)) || ((dotDotPos >= 0) && extSet.contains(filenm.substring(dotDotPos)));
}
