{
  super(importer != null ? importer.getFormatName() : Localization.lang("Autodetect format"));
  this.importer=importer;
  this.frame=frame;
  this.openInNew=openInNew;
  addActionListener(this);
}
