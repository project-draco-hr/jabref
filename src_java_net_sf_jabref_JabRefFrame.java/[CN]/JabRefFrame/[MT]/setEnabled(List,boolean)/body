{
  Iterator i=l.iterator();
  while (i.hasNext()) {
    Object o=i.next();
    if (o instanceof Action)     ((Action)o).setEnabled(enabled);
    if (o instanceof Component)     ((Component)o).setEnabled(enabled);
  }
}
