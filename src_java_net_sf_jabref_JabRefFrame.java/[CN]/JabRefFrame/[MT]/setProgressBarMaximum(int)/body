{
  if (SwingUtilities.isEventDispatchThread())   progressBar.setMaximum(value);
 else   SwingUtilities.invokeLater(new Runnable(){
    public void run(){
      progressBar.setMaximum(value);
    }
  }
);
}
