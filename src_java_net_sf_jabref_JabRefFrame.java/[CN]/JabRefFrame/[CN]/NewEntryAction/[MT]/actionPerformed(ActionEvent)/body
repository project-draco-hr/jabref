{
  if (tabbedPane.getTabCount() > 0)   ((BasePanel)(tabbedPane.getSelectedComponent())).newEntry(BibtexEntryType.getType(type));
 else   Util.pr("Action 'New entry' must be disabled when no " + "database is open.");
}
