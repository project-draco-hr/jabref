{
  if (tabbedPane.getTabCount() > 0)   ((BasePanel)(tabbedPane.getSelectedComponent())).runCommand(command);
 else   Util.pr("Action '" + command + "' must be disabled when no "+ "database is open.");
}
