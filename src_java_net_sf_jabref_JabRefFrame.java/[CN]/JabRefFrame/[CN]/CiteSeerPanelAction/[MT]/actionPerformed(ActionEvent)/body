{
  if (tabbedPane.getTabCount() > 0) {
    basePanel().sidePaneManager.togglePanel("CiteSeerPanel");
    if (basePanel().sidePaneManager.isPanelVisible("CiteSeerPanel"))     new FocusRequester(basePanel().medlineFetcher.getTextField());
  }
}
