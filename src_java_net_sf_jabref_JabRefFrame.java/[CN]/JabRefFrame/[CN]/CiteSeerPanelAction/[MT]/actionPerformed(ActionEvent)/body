{
  if (tabbedPane.getTabCount() > 0) {
    sidePaneManager.togglePanel("CiteSeerPanel");
    if (sidePaneManager.isPanelVisible("CiteSeerPanel"))     new FocusRequester(medlineFetcher.getTextField());
  }
}
