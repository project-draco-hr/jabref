{
  if (tabbedPane.getTabCount() > 0)   for (int i=0; i < tabbedPane.getTabCount(); i++) {
    ((BasePanel)tabbedPane.getComponentAt(i)).sidePaneManager.ensureVisible("fetchAuthorMedline");
    new FocusRequester(basePanel().medlineFetcher);
  }
}
