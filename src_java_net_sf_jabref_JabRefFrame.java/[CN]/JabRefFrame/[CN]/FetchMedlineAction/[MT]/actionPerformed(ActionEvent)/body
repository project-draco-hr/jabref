{
  if (tabbedPane.getTabCount() > 0) {
    sidePaneManager.togglePanel("fetchMedline");
    if (sidePaneManager.isPanelVisible("fetchMedline"))     new FocusRequester(medlineFetcher.getTextField());
  }
}
