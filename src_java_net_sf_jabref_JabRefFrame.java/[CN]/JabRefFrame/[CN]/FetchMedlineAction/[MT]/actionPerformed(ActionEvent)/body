{
  if (tabbedPane.getTabCount() > 0) {
    basePanel().sidePaneManager.togglePanel("fetchMedline");
    if (basePanel().sidePaneManager.isPanelVisible("fetchMedline"))     new FocusRequester(basePanel().medlineFetcher.getTextField());
  }
}
