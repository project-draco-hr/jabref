{
  basePanel().cleanUp();
  tabbedPane.remove(basePanel());
  if (tabbedPane.getTabCount() > 0) {
    markActiveBasePanel();
  }
  updateEnabledState();
  output(Globals.lang("Closed database") + ".");
  System.gc();
}
