{
  int i;
  for (i=0; i <= lastElement && data[i] != o; i++) {
    ;
  }
  if (i <= lastElement) {
    data[i]=null;
    int above=lastElement - i;
    if (above > 0) {
      System.arraycopy(data,i + 1,data,i,above);
    }
    lastElement--;
    return true;
  }
 else {
    return false;
  }
}
