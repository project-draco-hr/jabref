{
  for (int i=0; i < bibs.size(); i++) {
    BibtexEntry curEntry=(BibtexEntry)bibs.get(i);
    if (curEntry.getField(Globals.OWNER) == null || ((String)curEntry.getField(Globals.OWNER)).length() == 0) {
      curEntry.setField(Globals.OWNER,defaultOwner);
    }
  }
}
