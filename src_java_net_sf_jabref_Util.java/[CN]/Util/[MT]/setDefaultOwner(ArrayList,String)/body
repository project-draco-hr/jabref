{
  for (int i=0; i < bibs.size(); i++) {
    BibtexEntry curEntry=(BibtexEntry)bibs.get(i);
    if (curEntry.getField("owner") == null || ((String)curEntry.getField("owner")).length() == 0) {
      curEntry.setField("owner",defaultOwner);
    }
  }
}
