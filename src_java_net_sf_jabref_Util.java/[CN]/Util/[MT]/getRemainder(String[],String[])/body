{
  ArrayList<String> al=new ArrayList<String>();
  for (  String anAll : all) {
    boolean found=false;
    inner:     for (    String aSubset : subset) {
      if (aSubset.equals(anAll)) {
        found=true;
        break inner;
      }
    }
    if (!found)     al.add(anAll);
  }
  return al.toArray(new String[al.size()]);
}
