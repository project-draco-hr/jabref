{
  StringBuffer sb=new StringBuffer();
  char c;
  for (int i=0; i < s.length(); ++i) {
    c=s.charAt(i);
    if (specials.indexOf(c) >= 0 || c == quoteChar)     sb.append(quoteChar);
    sb.append(c);
  }
  return sb.toString();
}
