{
  StringBuffer out=new StringBuffer(in.replaceAll("[ \\t\\n\\r]+"," "));
  int p=in.length() - wrapAmount;
  while (p > 0) {
    p=out.lastIndexOf(" ",p);
    if (p <= 0 || p <= 20)     break;
 else {
      out.insert(p,"\n\t");
    }
    p-=wrapAmount;
  }
  return out.toString();
}
