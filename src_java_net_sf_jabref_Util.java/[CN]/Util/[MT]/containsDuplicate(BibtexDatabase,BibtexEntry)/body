{
  Collection entries=database.getEntries();
  for (Iterator i=entries.iterator(); i.hasNext(); ) {
    BibtexEntry other=(BibtexEntry)i.next();
    if (isDuplicate(entry,other,Globals.duplicateThreshold))     return other;
  }
  return null;
}
