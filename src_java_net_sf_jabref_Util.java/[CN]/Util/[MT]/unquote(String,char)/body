{
  StringBuffer sb=new StringBuffer();
  char c;
  for (int i=0; i < s.length(); ++i) {
    c=s.charAt(i);
    if (c != quoteChar)     sb.append(c);
 else     if (i + 1 < s.length() && s.charAt(i + 1) == '\n')     ++i;
  }
  return sb.toString();
}
