{
  if (field.equals("bibtextype"))   return bibtex.getType().getName();
  String res=(String)bibtex.getField(field);
  if ((res != null) && (database != null))   res=database.resolveForStrings(res);
  return res;
}
