{
  String scheme="http";
  String ssp;
  if (link.indexOf("//") > 0)   ssp="//" + link.substring(2 + link.indexOf("//"));
 else   ssp="//" + link;
  ssp=ssp.replaceAll("%20"," ");
  URI uri=new URI(scheme,ssp,null);
  return uri.toASCIIString();
}
