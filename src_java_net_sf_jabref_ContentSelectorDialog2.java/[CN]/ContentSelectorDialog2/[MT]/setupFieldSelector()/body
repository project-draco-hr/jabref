{
  fieldListModel.clear();
  for (Iterator i=metaData.iterator(); i.hasNext(); ) {
    String s=(String)i.next();
    if (s.startsWith(Globals.SELECTOR_META_PREFIX))     fieldListModel.addElement(s.substring(Globals.SELECTOR_META_PREFIX.length()));
  }
}
