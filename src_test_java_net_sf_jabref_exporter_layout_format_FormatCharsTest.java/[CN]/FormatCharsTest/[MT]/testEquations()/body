{
  LayoutFormatter layout=new FormatChars();
  Assert.assertEquals("$",layout.format("\\$"));
  Assert.assertEquals("??",layout.format("$\\sigma$"));
  Assert.assertEquals("A 32\u00A0mA ????-modulator",layout.format("A 32~{mA} {$\\Sigma\\Delta$}-modulator"));
}
