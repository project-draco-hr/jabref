{
  List<String> result=data.get(key);
  if (result == null) {
    result=getDefaultValue();
    if (result == null) {
      return LabelPatternUtil.DEFAULT_LABELPATTERN;
    }
  }
  return result;
}
