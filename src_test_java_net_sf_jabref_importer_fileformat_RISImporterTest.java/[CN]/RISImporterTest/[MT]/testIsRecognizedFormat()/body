{
  RisImporter importer=new RisImporter();
  try (InputStream stream=RISImporterTest.class.getResourceAsStream("RisImporterTest1.ris")){
    Assert.assertTrue(importer.isRecognizedFormat(stream));
  }
 }
