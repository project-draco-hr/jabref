{
  if (!base.hasStringLabel(name))   return null;
  for (Iterator i=base.getStringKeySet().iterator(); i.hasNext(); ) {
    BibtexString bs=base.getString(i.next());
    if (bs.getName().equals(name) && !used.contains("" + i)) {
      used.add("" + i);
      return bs;
    }
  }
  return null;
}
