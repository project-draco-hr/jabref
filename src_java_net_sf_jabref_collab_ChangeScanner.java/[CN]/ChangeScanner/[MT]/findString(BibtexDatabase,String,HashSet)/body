{
  if (!base.hasStringLabel(name))   return -1;
  for (int i=0; i < base.getStringCount(); i++) {
    BibtexString bs=base.getString(i);
    if (bs.getName().equals(name) && !used.contains("" + i)) {
      used.add("" + i);
      return i;
    }
  }
  return -1;
}
