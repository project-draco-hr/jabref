{
  if (dirsOnly) {
    Path path=new NewFileDialogs(frame,comp.getText()).dirsOnly().withExtensions(extensions).openDlgAndGetSelectedFile().orElse(Paths.get(""));
    String file=path.toString();
    return file;
  }
 else {
    Path path=new NewFileDialogs(frame,comp.getText()).withExtensions(extensions).openDlgAndGetSelectedFile().orElse(Paths.get(""));
    String file=path.toString();
    return file;
  }
}
