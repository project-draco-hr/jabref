{
  if (dir) {
    return FileDialogs.getNewDir(frame,new File(comp.getText()),extension,JFileChooser.OPEN_DIALOG,false);
  }
 else {
    return FileDialogs.getNewFile(frame,new File(comp.getText()),extension,JFileChooser.OPEN_DIALOG,false);
  }
}
