{
  if (col < PADLEFT)   return false;
  try {
    if (!getColumnName(col).toLowerCase().equals(GUIGlobals.TYPE_HEADER))     return true;
 else     return false;
  }
 catch (  NullPointerException ex) {
    return false;
  }
}
