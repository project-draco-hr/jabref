{
synchronized (this) {
    if (c != null) {
      c.getDocument().removeDocumentListener(d);
      c=null;
      d=null;
    }
  }
  if (!e.isTemporary()) {
    entryEditorTab.getParent().updateField(e.getSource());
  }
}
