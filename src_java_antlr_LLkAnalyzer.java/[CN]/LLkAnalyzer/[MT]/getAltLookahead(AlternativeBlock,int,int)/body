{
  Lookahead p;
  Alternative a=blk.getAlternativeAt(alt);
  AlternativeElement e=a.head;
  if (a.cache[k] == null) {
    p=e.look(k);
    a.cache[k]=p;
  }
 else {
    p=a.cache[k];
  }
  return p;
}
