{
  if (aDatabase == null)   throw new IllegalArgumentException("Passing a 'null' BibtexDatabase.");
  entries=aDatabase.getEntries();
  possibleFilePaths=JabRef.jrf.basePanel().metaData().getFileDirectory(GUIGlobals.FILE_FIELD);
}
