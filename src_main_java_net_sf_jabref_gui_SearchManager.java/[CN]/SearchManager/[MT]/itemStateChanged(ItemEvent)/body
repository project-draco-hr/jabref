{
  if (e.getSource() == increment) {
    if (startedFilterSearch || startedFloatSearch) {
      clearSearch();
    }
    updateSearchButtonText();
    updateKeyListeners();
  }
 else {
    updateSearchButtonText();
  }
}
