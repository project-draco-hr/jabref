{
  String res="";
switch (dbtype) {
case MYSQL:
    res=processDMLWithSingleResult(conn,"SELECT label FROM group_types WHERE group_types_id=\"" + groupId + "\";");
  break;
case POSTGRESQL:
res=processDMLWithSingleResult(conn,"SELECT label FROM group_types WHERE group_types_id=\'" + groupId + "\';");
break;
default :
}
return res;
}
