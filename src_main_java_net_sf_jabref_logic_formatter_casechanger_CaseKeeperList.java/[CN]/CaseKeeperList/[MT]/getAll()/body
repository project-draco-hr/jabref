{
  int length=0;
  for (  String[] array : allLists) {
    length+=array.length;
  }
  List<String> result=new ArrayList<>(length);
  for (  String[] array : allLists) {
    result.addAll(Arrays.asList(array));
  }
  return result;
}
