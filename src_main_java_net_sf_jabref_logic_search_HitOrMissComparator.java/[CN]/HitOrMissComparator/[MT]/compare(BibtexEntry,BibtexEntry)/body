{
  if (hitOrMiss == null) {
    return 0;
  }
  boolean hit1=hitOrMiss.matches(o1);
  boolean hit2=hitOrMiss.matches(o2);
  if (hit1 == hit2) {
    return 0;
  }
 else {
    return hit1 ? -1 : 1;
  }
}
