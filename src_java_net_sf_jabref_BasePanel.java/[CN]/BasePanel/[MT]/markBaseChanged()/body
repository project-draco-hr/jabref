{
  baseChanged=true;
  String oldTitle=frame.getTabTitle(this);
  if (!oldTitle.endsWith("*"))   frame.setTabTitle(this,oldTitle + "*");
  if (frame.statusLine.getText().startsWith("Saved database"))   frame.output(" ");
}
