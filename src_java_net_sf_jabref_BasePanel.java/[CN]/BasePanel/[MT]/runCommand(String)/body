{
  final String command=_command;
  if (actions.get(command) == null)   Util.pr("No action defined for'" + command + "'");
 else {
    Object o=actions.get(command);
    try {
      if (o instanceof BaseAction)       ((BaseAction)o).action();
 else       ((AbstractWorker)o).getWorker().run();
    }
 catch (    Throwable ex) {
      ex.printStackTrace();
    }
  }
}
