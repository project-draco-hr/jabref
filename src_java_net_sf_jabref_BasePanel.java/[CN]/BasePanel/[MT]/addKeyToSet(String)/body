{
  boolean exists=false;
  if (key.equals(""))   return false;
  if (allKeys.containsKey(key)) {
    exists=true;
    allKeys.put(key,new Integer(((Integer)allKeys.get(key)).intValue() + 1));
  }
 else   allKeys.put(key,new Integer(1));
  return exists;
}
