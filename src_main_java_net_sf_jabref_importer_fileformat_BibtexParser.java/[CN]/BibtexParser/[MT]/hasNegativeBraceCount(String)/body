{
  int index=0;
  int braceCount=0;
  while (index < toCheck.length()) {
    if (toCheck.charAt(index) == '{') {
      braceCount++;
    }
 else     if (toCheck.charAt(index) == '}') {
      braceCount--;
    }
    if (braceCount < 0) {
      return true;
    }
    index++;
  }
  return false;
}
