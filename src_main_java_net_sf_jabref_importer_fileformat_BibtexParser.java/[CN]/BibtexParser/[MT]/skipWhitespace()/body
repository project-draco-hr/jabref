{
  int c;
  while (true) {
    c=read();
    if (isEOFCharacter(c)) {
      eof=true;
      return;
    }
    if (Character.isWhitespace((char)c)) {
      continue;
    }
 else {
      unread(c);
      break;
    }
  }
}
