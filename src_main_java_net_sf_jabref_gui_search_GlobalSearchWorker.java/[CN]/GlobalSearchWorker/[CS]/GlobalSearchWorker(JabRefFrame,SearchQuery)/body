{
  this.frame=Objects.requireNonNull(frame);
  this.searchQuery=Objects.requireNonNull(query);
  dialog=new SearchResultsDialog(frame,Localization.lang("Search results in all databases for %0",SearchQueryLocalizer.localize(this.searchQuery)));
}
