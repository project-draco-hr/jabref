{
  disableSystemExit();
  try {
    String[] args={"-p"," ",PATH_TO_TEST_BIBTEX};
    JabRef.main(args);
  }
 catch (  ExitException ignored) {
  }
 finally {
    enableSystemExit();
  }
  JabRef jabref=JabRef.singleton;
  return jabref;
}
