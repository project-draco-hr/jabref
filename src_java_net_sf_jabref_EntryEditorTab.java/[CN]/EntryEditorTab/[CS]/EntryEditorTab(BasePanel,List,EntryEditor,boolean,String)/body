{
  if (fields != null)   this.fields=(String[])fields.toArray(new String[0]);
 else   this.fields=new String[]{};
  this.parent=parent;
  setupPanel(panel,addKeyField,name);
  panel.setFocusCycleRoot(true);
}
