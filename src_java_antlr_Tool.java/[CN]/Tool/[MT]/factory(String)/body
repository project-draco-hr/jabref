{
  Class c;
  Object o=null;
  try {
    c=Class.forName(p);
    o=c.newInstance();
  }
 catch (  Exception e) {
    warning("Can't create an object of type " + p);
    return null;
  }
  return o;
}
