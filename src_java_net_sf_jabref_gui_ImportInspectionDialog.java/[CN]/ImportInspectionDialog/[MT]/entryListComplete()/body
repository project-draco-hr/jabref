{
  progressBar.setIndeterminate(false);
  progressBar.setVisible(false);
  ok.setEnabled(true);
  if (!generatedKeys)   generate.setEnabled(true);
  stop.setEnabled(false);
  this.glTable.repaint();
  if (this.glTable.getSelectedRowCount() == 0)   this.glTable.setRowSelectionInterval(0,0);
}
