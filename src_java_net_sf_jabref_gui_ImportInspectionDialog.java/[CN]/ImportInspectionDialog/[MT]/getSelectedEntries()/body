{
  List selected=new ArrayList();
  for (Iterator i=entries.iterator(); i.hasNext(); ) {
    BibtexEntry entry=(BibtexEntry)i.next();
    if (entry.isSearchHit())     selected.add(entry);
  }
  return selected;
}
