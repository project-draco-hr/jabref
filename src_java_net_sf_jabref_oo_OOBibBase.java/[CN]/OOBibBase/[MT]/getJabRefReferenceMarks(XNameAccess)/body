{
  String[] names=nameAccess.getElementNames();
  ArrayList<String> tmp=new ArrayList<String>();
  for (int i=0; i < names.length; i++) {
    if (citePattern.matcher(names[i]).find())     tmp.add(names[i]);
  }
  names=tmp.toArray(new String[tmp.size()]);
  return names;
}
