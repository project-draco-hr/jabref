{
  skipWhitespace();
  consume('{','(');
  skipWhitespace();
  String name=parseTextToken();
  skipWhitespace();
  consume('=');
  String content=parseFieldContent();
  consume('}',')');
  return new BibtexString(name,content);
}
