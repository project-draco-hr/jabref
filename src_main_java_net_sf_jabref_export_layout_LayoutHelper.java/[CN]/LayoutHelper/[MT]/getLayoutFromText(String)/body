{
  parse();
  StringInt si;
  for (  StringInt parsedEntry : parsedEntries) {
    si=parsedEntry;
    if ((si.i == LayoutHelper.IS_SIMPLE_FIELD) || (si.i == LayoutHelper.IS_FIELD_START) || (si.i == LayoutHelper.IS_FIELD_END)|| (si.i == LayoutHelper.IS_GROUP_START)|| (si.i == LayoutHelper.IS_GROUP_END)) {
      si.s=si.s.trim().toLowerCase();
    }
  }
  return new Layout(parsedEntries,classPrefix);
}
