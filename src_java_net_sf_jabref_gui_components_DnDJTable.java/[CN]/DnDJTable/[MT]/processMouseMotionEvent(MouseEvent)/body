{
  super.processMouseMotionEvent(e);
  if (e.getID() == MouseEvent.MOUSE_DRAGGED) {
    clearSelection();
    for (int i=0; i < selectionToDrag.length; ++i)     addRowSelectionInterval(selectionToDrag[i],selectionToDrag[i]);
  }
}
