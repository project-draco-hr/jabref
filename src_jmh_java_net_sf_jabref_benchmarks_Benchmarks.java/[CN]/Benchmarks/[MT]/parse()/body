{
  StringReader bibtexStringReader=new StringReader(bibtexString);
  BibtexParser parser=new BibtexParser(bibtexStringReader,ImportFormatPreferences.fromPreferences(Globals.prefs));
  return parser.parse();
}
