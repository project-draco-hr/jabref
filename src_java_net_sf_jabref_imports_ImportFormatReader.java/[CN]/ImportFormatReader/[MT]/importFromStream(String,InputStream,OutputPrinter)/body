{
  ImportFormat importer=getByCliId(format);
  if (importer == null)   throw new IllegalArgumentException("Unknown import format: " + format);
  List<BibtexEntry> res=importer.importEntries(in,status);
  if (res != null)   purgeEmptyEntries(res);
  return res;
}
