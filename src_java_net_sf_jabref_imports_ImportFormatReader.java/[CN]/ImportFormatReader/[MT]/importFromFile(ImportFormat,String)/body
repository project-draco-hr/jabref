{
  File file=new File(filename);
  InputStream stream=new FileInputStream(file);
  if (!importer.isRecognizedFormat(stream))   return null;
  stream=new FileInputStream(file);
  return importer.importEntries(stream);
}
