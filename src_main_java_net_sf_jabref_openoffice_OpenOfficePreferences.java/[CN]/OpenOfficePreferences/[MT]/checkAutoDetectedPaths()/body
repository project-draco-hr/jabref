{
  if (preferences.hasKey(JabRefPreferences.OO_JARS_PATH) && preferences.hasKey(JabRefPreferences.OO_EXECUTABLE_PATH)) {
    return new File(getJarsPath(),"jurt.jar").exists() && new File(preferences.get(JabRefPreferences.OO_EXECUTABLE_PATH)).exists();
  }
 else {
    return false;
  }
}
