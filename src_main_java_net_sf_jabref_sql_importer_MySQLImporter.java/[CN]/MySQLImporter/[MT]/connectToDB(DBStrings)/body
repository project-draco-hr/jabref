{
  String url=SQLUtil.createJDBCurl(dbstrings,true);
  String drv="com.mysql.jdbc.Driver";
  Class.forName(drv).newInstance();
  return DriverManager.getConnection(url,dbstrings.getUsername(),dbstrings.getPassword());
}
