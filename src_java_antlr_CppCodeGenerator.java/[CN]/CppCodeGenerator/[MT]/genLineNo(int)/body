{
  if (line == 0) {
    line++;
  }
  if (genHashLines)   _println("#line " + line + " \""+ Tool.fileMinusPath(tool.grammarFile)+ "\"");
 else   println("// line " + line + " \""+ Tool.fileMinusPath(tool.grammarFile)+ "\"");
}
