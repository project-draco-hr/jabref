{
  if (usingCustomAST && ((grammar instanceof TreeWalkerGrammar) || (grammar instanceof ParserGrammar)) && !(grammar.tokenManager.tokenDefined(str))) {
    return namespaceAntlr + "RefAST(" + str+ ")";
  }
 else {
    return str;
  }
}
