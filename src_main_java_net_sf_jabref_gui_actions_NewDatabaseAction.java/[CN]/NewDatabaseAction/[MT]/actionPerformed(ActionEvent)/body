{
  BibtexDatabase database=new BibtexDatabase();
  jabRefFrame.addTab(database,null,new MetaData(),Charset.forName(Globals.prefs.get(JabRefPreferences.DEFAULT_ENCODING)),true);
  jabRefFrame.output(Localization.lang("New database created."));
}
