{
  BibDatabase database=new BibDatabase();
  jabRefFrame.addTab(database,null,new MetaData(),Globals.prefs.getDefaultEncoding(),true);
  jabRefFrame.output(Localization.lang("New database created."));
}
