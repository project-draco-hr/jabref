{
  if (field != null) {
    String data=field.getSelectedText();
    if (data != null) {
      if (!data.isEmpty()) {
        ClipBoardManager.clipBoard.setClipboardContents(data);
      }
    }
  }
}
