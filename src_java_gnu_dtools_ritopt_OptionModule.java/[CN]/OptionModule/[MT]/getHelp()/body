{
  String retval="";
  Collection col=options.values();
  Iterator it=col.iterator();
  while (it.hasNext()) {
    Option next=(Option)it.next();
    retval+=next.getHelp() + "\n";
  }
  return retval;
}
