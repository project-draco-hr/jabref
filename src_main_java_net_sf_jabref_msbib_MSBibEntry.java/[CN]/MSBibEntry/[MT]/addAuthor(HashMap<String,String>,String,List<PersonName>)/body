{
  if (authorsLst == null)   return;
  String allAuthors="";
  boolean First=true;
  for (  PersonName name : authorsLst) {
    if (!First)     allAuthors+=" and ";
    allAuthors+=name.getFullname();
    First=false;
  }
  hm.put(type,allAuthors);
}
