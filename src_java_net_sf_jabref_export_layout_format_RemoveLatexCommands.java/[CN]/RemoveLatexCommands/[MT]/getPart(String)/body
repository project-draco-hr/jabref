{
  char c;
  boolean found=false;
  StringBuffer part=new StringBuffer();
  while (!found && (i < text.length())) {
    i++;
    c=text.charAt(i);
    if (c == '}')     found=true;
 else     part.append((char)c);
  }
  return part.toString();
}
