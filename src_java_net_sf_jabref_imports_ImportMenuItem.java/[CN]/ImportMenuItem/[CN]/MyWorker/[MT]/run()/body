{
  if (!fileOk)   return;
  try {
    entries=Globals.importFormatReader.importFromFile(importer,filename);
    if (entries != null)     ImportFormatReader.purgeEmptyEntries(entries);
  }
 catch (  IOException ex) {
    ex.printStackTrace();
  }
}
