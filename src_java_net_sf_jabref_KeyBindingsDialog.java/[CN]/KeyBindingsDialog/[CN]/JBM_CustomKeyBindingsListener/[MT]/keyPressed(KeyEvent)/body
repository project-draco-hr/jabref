{
  Object[] selected=list.getSelectedValues();
  if (selected.length == 0)   return;
  String code=KeyEvent.getKeyText(evt.getKeyCode());
  String mod=KeyEvent.getKeyModifiersText(evt.getModifiers());
  if (mod.equals(""))   return;
  if (evt.isActionKey() || code.equals("Escape") || code.equals("Tab")|| code.equals("Backspace")|| code.equals("Enter")|| code.equals("Delete")|| code.equals("Space")|| code.equals("Ctrl")|| code.equals("Shift")|| code.equals("Alt"))   return;
  String newKey=mod.toLowerCase() + " " + code;
  keyTF.setText(newKey);
  String selectedFunction=(String)list.getSelectedValue();
  bindHM.put(selectedFunction,newKey);
}
