{
  int i=1;
  int level=0;
  loop:   do {
    char c=s.charAt(i);
switch (c) {
case '{':
      level++;
    break;
case '}':
  level--;
if (level == -1) {
  break loop;
}
break;
}
i++;
}
 while (i < s.length() - 1);
return (level == 0);
}
