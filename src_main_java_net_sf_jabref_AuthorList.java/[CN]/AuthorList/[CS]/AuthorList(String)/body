{
  authors=new Vector<>(5);
  orig=bibtex_authors;
  token_start=0;
  token_end=0;
  while (token_start < orig.length()) {
    Author author=getAuthor();
    if (author != null) {
      authors.add(author);
    }
  }
  orig=null;
  tokens=null;
}
