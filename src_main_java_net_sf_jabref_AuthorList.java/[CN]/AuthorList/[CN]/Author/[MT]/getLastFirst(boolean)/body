{
  String res=getLastOnly();
  if (jr_part != null) {
    res+=", " + jr_part;
  }
  if (abbr) {
    if (first_abbr != null) {
      res+=", " + first_abbr;
    }
  }
 else {
    if (first_part != null) {
      res+=", " + first_part;
    }
  }
  return res;
}
