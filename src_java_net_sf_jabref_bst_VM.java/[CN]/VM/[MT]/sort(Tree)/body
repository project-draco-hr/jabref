{
  Collections.sort(entries,new Comparator<BstEntry>(){
    public int compare(    BstEntry o1,    BstEntry o2){
      return ((String)o1.strings.get("sort.key$")).compareTo((String)o2.strings.get("sort.key$"));
    }
  }
);
}
