{
  if (name.length() <= 1) {
    return OTHER;
  }
  if (!(name.charAt(1) + "").toUpperCase().equals((name.charAt(1) + ""))) {
    return OTHER;
  }
  for (  Type t : Type.values()) {
    if (t.prefix.equals(name.charAt(0) + "")) {
      return t;
    }
  }
  return OTHER;
}
