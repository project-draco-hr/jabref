{
  try (Statement statement=(Statement)SQLUtil.processQueryWithResults(conn,"SELECT column_name FROM information_schema.columns WHERE table_name ='entries';");ResultSet rsColumns=statement.getResultSet()){
    List<String> colNames=new ArrayList<>();
    while (rsColumns.next()) {
      colNames.add(rsColumns.getString(1));
    }
    return colNames;
  }
 }
