{
  Object _obj=get(key);
  if (_obj == null) {
    if (parent != null) {
      _obj=parent.getValue(key);
    }
    if (_obj == null) {
      System.err.println("Warning: I could not find the label \'" + key + "\' and gave up");
    }
  }
  return (ArrayList)_obj;
}
