{
  if (parserResult.hasWarnings()) {
    if (jabRefFrame != null) {
      jabRefFrame.showBasePanelAt(dbCount);
    }
    JOptionPane.showMessageDialog(jabRefFrame,generateString(),generateDialogTitle(),JOptionPane.WARNING_MESSAGE);
  }
}
