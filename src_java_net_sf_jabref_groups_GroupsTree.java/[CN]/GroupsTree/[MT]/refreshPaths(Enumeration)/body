{
  Vector freshPaths=new Vector();
  while (paths.hasMoreElements()) {
    freshPaths.add(new TreePath(((DefaultMutableTreeNode)((TreePath)paths.nextElement()).getLastPathComponent()).getPath()));
  }
  return freshPaths.elements();
}
