{
  if (editor == null) {
    editor=new FileListEntryEditor(frame,entry);
  }
 else   editor.setEntry(entry);
  editor.setVisible(true);
  if (editor.okPressed())   tableModel.fireTableDataChanged();
  return editor.okPressed();
}
