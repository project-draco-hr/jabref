{
  Boolean b1=e1.hasField(FieldName.CROSSREF);
  Boolean b2=e2.hasField(FieldName.CROSSREF);
  if ((!b1) && (!b2)) {
    return 0;
  }
  if (b1 && b2) {
    return 0;
  }
  if (!b1) {
    return 1;
  }
 else {
    return -1;
  }
}
