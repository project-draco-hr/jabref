{
  Objects.requireNonNull(databaseContext);
  Objects.requireNonNull(entries);
  if (!entries.isEmpty()) {
    new DBExporterAndImporterFactory().getExporter("POSTGRESQL").exportDatabaseAsFile(databaseContext,entries,file,encoding);
  }
}
