{
  Objects.requireNonNull(databaseContext);
  Objects.requireNonNull(entries);
  if (!entries.isEmpty()) {
    new DBExporterAndImporterFactory().getExporter(DatabaseType.POSTGRESQL).exportDatabaseAsFile(databaseContext,entries,file,encoding);
  }
}
