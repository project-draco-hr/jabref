{
  lock();
  try {
    int index=list.indexOf(entryChangedEvent.getBibEntry());
    if (index != -1) {
      list.set(index,entryChangedEvent.getBibEntry());
    }
  }
  finally {
    unlock();
  }
}
