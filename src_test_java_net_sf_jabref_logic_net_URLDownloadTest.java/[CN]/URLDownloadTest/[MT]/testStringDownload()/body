{
  URLDownload dl=new URLDownload(new URL("http://www.google.com"));
  Assert.assertTrue("google.com should contain google",dl.downloadToString(JabRefPreferences.getInstance().getDefaultEncoding()).contains("Google"));
}
