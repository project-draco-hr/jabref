{
  BufferedReader in=new BufferedReader(ImportFormatReader.getReaderDefaultEncoding(stream));
  String str;
  while ((str=in.readLine()) != null) {
    if ((str.length() >= 5) && (str.substring(0,5).equals("AU:  ")))     return true;
  }
  return false;
}
