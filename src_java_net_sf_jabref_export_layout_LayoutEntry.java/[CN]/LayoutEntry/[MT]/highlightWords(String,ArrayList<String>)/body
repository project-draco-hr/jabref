{
  if (toHighlight == null)   return text;
  if (Character.isLetterOrDigit(text.charAt(0))) {
    for (    String word : toHighlight) {
      text=highlightWord(text,word);
    }
  }
  return text;
}
