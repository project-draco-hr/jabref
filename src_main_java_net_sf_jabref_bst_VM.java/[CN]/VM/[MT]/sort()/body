{
  Collections.sort(entries,new Comparator<BstEntry>(){
    @Override public int compare(    BstEntry o1,    BstEntry o2){
      return (o1.localStrings.get("sort.key$")).compareTo(o2.localStrings.get("sort.key$"));
    }
  }
);
}
