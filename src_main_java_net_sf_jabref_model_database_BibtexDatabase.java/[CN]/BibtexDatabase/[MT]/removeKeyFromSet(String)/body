{
  if ((key == null) || key.isEmpty()) {
    return;
  }
  if (allKeys.containsKey(key)) {
    Integer tI=allKeys.get(key);
    if (tI == 1) {
      allKeys.remove(key);
    }
 else {
      allKeys.put(key,tI - 1);
    }
  }
}
