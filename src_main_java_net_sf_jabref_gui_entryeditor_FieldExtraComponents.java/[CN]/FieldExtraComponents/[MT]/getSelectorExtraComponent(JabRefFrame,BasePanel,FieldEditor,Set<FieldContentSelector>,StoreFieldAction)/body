{
  FieldContentSelector ws=new FieldContentSelector(frame,panel,frame,editor,panel.getLoadedDatabase().getMetaData(),storeFieldAction,false,"author".equals(editor.getFieldName()) || "editor".equals(editor.getFieldName()) ? " and " : ", ");
  contentSelectors.add(ws);
  return Optional.of(ws);
}
