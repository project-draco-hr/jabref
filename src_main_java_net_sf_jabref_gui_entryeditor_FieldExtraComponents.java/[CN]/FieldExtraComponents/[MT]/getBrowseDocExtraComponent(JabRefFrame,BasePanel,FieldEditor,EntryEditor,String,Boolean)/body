{
  OpenFileFilter off=Util.getFileFilterForField(fieldName,isZip);
  return Optional.of(new ExternalFilePanel(frame,panel.getBibDatabaseContext().getMetaData(),entryEditor,fieldName,off,fieldEditor));
}
