{
  BibtexParser parser=new BibtexParser(new StringReader("@article{test,author={Ed von Test}}"),importFormatPreferences);
  ParserResult result=parser.parse();
  assertEquals(result,parser.parse());
}
