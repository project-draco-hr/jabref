{
  StringWriter sw=new StringWriter();
  new BibEntryWriter(new LatexFieldFormatter(LatexFieldFormatterPreferences.fromPreferences(Globals.prefs)),false).write(e,sw,BibDatabaseMode.BIBTEX);
  return sw.getBuffer().toString();
}
