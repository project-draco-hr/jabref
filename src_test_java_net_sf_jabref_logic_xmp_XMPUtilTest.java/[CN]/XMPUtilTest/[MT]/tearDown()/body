{
  if (!pdfFile.delete()) {
    System.err.println("Note: Cannot delete temporary file (already deleted so the corresponding test passed).");
  }
  prefs.putBoolean("useXmpPrivacyFilter",use);
  prefs.putStringList(JabRefPreferences.XMP_PRIVACY_FILTERS,privacyFilters);
}
