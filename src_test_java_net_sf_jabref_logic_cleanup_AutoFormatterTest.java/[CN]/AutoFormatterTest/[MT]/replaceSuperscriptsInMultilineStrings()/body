{
  entry.setField("multiline","replace on 1st line\nand on 2nd line.");
  new AutoFormatter(entry).applySuperscripts();
  Assert.assertEquals("replace on 1\\textsuperscript{st} line\nand on 2\\textsuperscript{nd} line.",entry.getField("multiline"));
}
