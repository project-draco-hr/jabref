{
  selected.setEnabled(selection);
  if (selection)   selected.setSelected(true);
 else   all.setSelected(true);
  if (!set.isSelected() && !clear.isSelected() && !rename.isSelected())   set.setSelected(true);
}
