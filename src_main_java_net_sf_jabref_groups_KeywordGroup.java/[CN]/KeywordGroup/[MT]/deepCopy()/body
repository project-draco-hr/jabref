{
  try {
    return new KeywordGroup(m_name,searchField,searchExpression,caseSensitive,regExp,m_context);
  }
 catch (  Throwable t) {
    System.err.println("Internal error: Exception " + t + " in KeywordGroup.deepCopy(). "+ "Please report this on www.sf.net/projects/jabref");
    return null;
  }
}
