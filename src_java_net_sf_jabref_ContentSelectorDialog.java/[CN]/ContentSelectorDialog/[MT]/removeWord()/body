{
  String word=wordTf.getText().trim().toLowerCase();
  if (wordSet.contains(word)) {
    wordSet.remove(word);
    metaData.putData(Globals.SELECTOR_META_PREFIX + currentField,new Vector(wordSet));
    fillWordSelector();
    frame.basePanel().markNonUndoableBaseChanged();
    wordTf.setText("");
    wordTf.requestFocus();
  }
}
