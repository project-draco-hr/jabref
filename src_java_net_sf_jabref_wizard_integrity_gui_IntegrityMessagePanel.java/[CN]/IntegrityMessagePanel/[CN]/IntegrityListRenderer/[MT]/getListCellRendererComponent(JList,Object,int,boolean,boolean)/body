{
  super.getListCellRendererComponent(list,value,index,iss,chf);
  if (value != null) {
    int id=((IntegrityMessage)value).getType();
    if (id < 1000)     setIcon(infoIcon);
 else     if (id < 2000)     setIcon(warnIcon);
 else     setIcon(failIcon);
  }
  return this;
}
