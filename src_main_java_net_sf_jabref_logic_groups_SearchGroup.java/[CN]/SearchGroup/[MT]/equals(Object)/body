{
  if (!(o instanceof SearchGroup)) {
    return false;
  }
  SearchGroup other=(SearchGroup)o;
  return name.equals(other.name) && this.getSearchExpression().equals(other.getSearchExpression()) && (this.isCaseSensitive() == other.isCaseSensitive())&& (isRegExp() == other.isRegExp())&& (getHierarchicalContext() == other.getHierarchicalContext());
}
