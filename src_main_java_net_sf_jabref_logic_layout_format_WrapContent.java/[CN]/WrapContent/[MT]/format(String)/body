{
  if (fieldText == null) {
    return null;
  }
  if (before == null) {
    return fieldText;
  }
  if (fieldText.isEmpty()) {
    return "";
  }
 else {
    return before + fieldText + after;
  }
}
