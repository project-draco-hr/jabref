{
  String chosen=null;
  chosen=Globals.getNewFile(_frame,Globals.prefs,new File(comp.getText()),".aux",JFileChooser.OPEN_DIALOG,false);
  if (chosen != null) {
    File newFile=new File(chosen);
    comp.setText(newFile.getPath());
  }
}
