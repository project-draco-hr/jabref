{
  try {
    saxParser.parse(this.getClass().getResourceAsStream("oai23.xml"),handler);
    Assert.assertEquals("Javier L??pez Pe??a and Gabriel Navarro",be.getField("author"));
  }
 catch (  SAXException e) {
    throw e.getException();
  }
}
