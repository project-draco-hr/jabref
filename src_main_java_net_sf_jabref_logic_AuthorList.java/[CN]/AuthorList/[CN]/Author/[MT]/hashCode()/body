{
  final int prime=31;
  int result=1;
  result=prime * result + (firstAbbr == null ? 0 : firstAbbr.hashCode());
  result=prime * result + (firstPart == null ? 0 : firstPart.hashCode());
  result=prime * result + (jrPart == null ? 0 : jrPart.hashCode());
  result=prime * result + (lastPart == null ? 0 : lastPart.hashCode());
  result=prime * result + (vonPart == null ? 0 : vonPart.hashCode());
  return result;
}
