{
  String res=getLastOnly();
  if (jrPart != null) {
    res+=", " + jrPart;
  }
  if (abbr) {
    if (firstAbbr != null) {
      res+=", " + firstAbbr;
    }
  }
 else {
    if (firstPart != null) {
      res+=", " + firstPart;
    }
  }
  return res;
}
