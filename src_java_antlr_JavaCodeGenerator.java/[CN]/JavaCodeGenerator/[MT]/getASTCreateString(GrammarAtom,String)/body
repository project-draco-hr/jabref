{
  if (atom != null && atom.getASTNodeType() != null) {
    return "(" + atom.getASTNodeType() + ")"+ "astFactory.create("+ astCtorArgs+ ",\""+ atom.getASTNodeType()+ "\")";
  }
 else {
    return getASTCreateString(astCtorArgs);
  }
}
